- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.bash_profile: home/bash_profile
    ~/.oni/config.js:
    ~/.oni/init.vim:
    ~/.oni/ipy.vim:
    ~/.oni/vim_python.py:


- shell:
  - [git submodule update --init --recursive, Installing submodules]
